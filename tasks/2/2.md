# Задание 2

Реализовать REST API для управления списком контактов с хранением данных в
памяти.

## Функциональные требования

1. Для всех эндпоинтов сервис принимает и отдает данные в формате `JSON`.
   В ответе всегда проставлен заголовок
   `Content-Type: application/json`.

1. Пользователь может добавить новый контакт с помощью эндпоинта
   `POST http://127.0.0.1:8080/api/v1/contacts`.

   Пример тела запроса:

   ```json
   {
     "first_name": "Александр",
     "middle_name": "Сергеевич",
     "last_name": "Пушкин",
     "birthday": "1799-06-06",
     "description": "Великий поэт"
   }
   ```

   Пример тела ответа:

   ```json
   {
     "id": 1,
     "first_name": "Александр",
     "middle_name": "Сергеевич",
     "last_name": "Пушкин",
     "birthday": "1799-06-06",
     "description": "Великий поэт"
   }
   ```

1. Пользователь может получить список всех контактов с помощью
   эндпоинта `GET http://127.0.0.1:8080/api/v1/contacts`.

   Пример тела ответа:

   ```json
   [
     {
       "id": 1,
       "first_name": "Александр",
       "middle_name": "Сергеевич",
       "last_name": "Пушкин",
       "birthday": "1799-06-06",
       "description": "Великий поэт"
     },
     {
       "id": 2,
       "first_name": "Кот",
       "last_name": "Матроскин",
       "birthday": "1984-08-10",
       "description": "Кот из мультфильма"
     }
   ]
   ```

1. Пользователь может получить информацию о конкретном контакте с помощью
   эндпоинта `GET http://127.0.0.1:8080/api/v1/contacts/{id}`.

   Пример тела ответа:

   ```json
   {
     "id": 1,
     "first_name": "Александр",
     "middle_name": "Сергеевич",
     "last_name": "Пушкин",
     "birthday": "1799-06-06",
     "description": "Великий поэт"
   }
   ```

1. Пользователь может удалить контакт с помощью
   эндпоинта `DELETE http://127.0.0.1:8080/api/v1/contacts/{id}`. В случае успеха
   возвращается пустое тело ответа с кодом `204` (No Content).

1. Сервис должен возвращать ошибки с кодом `404` в случае попытки
   обратиться к контакту с несуществующим `ID` (получить или удалить контакт).

## Нефункциональные требования

1. Уникальный `ID` контакта генерируется на стороне сервиса. Выбор алгоритма
   генерации остается на усмотрение разработчика.

1. Контакты хранятся в оперативной памяти сервиса. Потеря списка контактов при
   перезапуске сервиса считается допустимой.

## Ограничения

1. Для построения `api` допускается использовать только библиотеку `net/http`
   и предоставляемый ею роутер.

## Материалы для ознакомления

- [JSON Encoding & Decoding in Golang](https://www.youtube.com/watch?v=Vr63uGL7NrU)

- [Документация `net/http`](https://pkg.go.dev/net/http)

- [Маршрутизация HTTP-серверов в Go 1.22](https://habr.com/ru/articles/768034/) —
  руководство по организации маршрутизации запросов с использованием новых
  возможностей языка.

- [How to Handle Errors in Go](https://www.jetbrains.com/guide/go/tutorials/handle_errors_in_go/introduction/) —
  описывает текущий подход по работе с ошибками в `Go`.

- [Как устроены словари в Go](https://www.youtube.com/watch?v=N4TJCdTaKUM) —
  подробный обзор устройства старых (до Go 1.24) и новых map, а также необходимая
  база для понимания их работы.

- [Memory management](https://go101.org/article/memory-block.html) — подробное
  описание управления памятью в `Go` (стек и куча).
